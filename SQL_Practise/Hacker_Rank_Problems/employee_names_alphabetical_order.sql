-- Question: We have an Employee table that has columns like employee_id, name,
-- months, and salary. We need to get all the employee names and print them
-- in alphabetical order.

SELECT name
FROM Employee
ORDER BY name ASC;
