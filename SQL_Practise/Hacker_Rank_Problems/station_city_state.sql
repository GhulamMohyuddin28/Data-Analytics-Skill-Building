-- Question: We have a STATION table that has columns like ID, CITY, STATE,
-- LAT_N (northern latitude), and LONG_W (western longitude). We need to
-- get only the CITY and STATE columns from this STATION table.

SELECT CITY, STATE
FROM STATION;
