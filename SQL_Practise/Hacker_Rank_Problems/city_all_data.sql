-- Question: We have a CITY table that has columns like ID, NAME, COUNTRYCODE,
-- DISTRICT, and POPULATION. We need to get all the columns and every
-- row that exists in this CITY table without any filter or condition.

SELECT *
FROM CITY;
