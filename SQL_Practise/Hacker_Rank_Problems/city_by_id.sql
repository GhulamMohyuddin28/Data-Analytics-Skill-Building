-- Question: We have a CITY table that has columns like ID, NAME, COUNTRYCODE,
-- DISTRICT, and POPULATION. We need to get all the columns of that one
-- specific city whose ID is 1661.

SELECT *
FROM CITY
WHERE ID=1661;
